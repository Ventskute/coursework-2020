let modalWindow = null;
let colorMap = {}; 
let lux = 0;
let context;
let agentData = null;
let currentSlide = null;
const canvasWidth = 960;
const canvasHeight = 540;

module.exports = {
    modalWindow,
    colorMap,
    lux,
    context,
    agentData,
    currentSlide,
    canvasWidth,
    canvasHeight,
}